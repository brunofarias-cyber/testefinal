services:
  - type: web
    name: bprojetos
    runtime: node
    buildCommand: npm install && npm run build:render
    startCommand: npm run start:prod
    envVars:
      - key: NODE_VERSION
        value: 20.16.11
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
